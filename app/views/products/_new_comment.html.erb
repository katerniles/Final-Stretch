<% if signed_in? %>
<p>Add a Review!</p>
<%= form_for ([@product, @product.comments.build]) do |f| %>
</p>
  <p>
    <%= f.label :body, "Comments"%> <br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.label :rating %><br>
    <%= f.text_field :rating, class: "rating" %>  </p>  
    <div class="rating"></div>
  <p>
     <%= f.submit "Submit" %>
  </p>
  <% end %>
<% end %>